# Grafana dashboard provisioning configuration

apiVersion: 1

providers:
  - name: 'inventory-api-dashboards'
    orgId: 1
    folder: 'Inventory Management'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
